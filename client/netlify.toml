[build]
  base    = "client"
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# React Router — send all routes to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
